{{!-- Setup the index.handlebars to have the template that Handlebars can render onto.

Create a button in index.handlebars that will submit the user input into the database. --}}

<h1>Eat da Burger!</h1>


<div class="results">
    <div class="namedBurgers">
        <ul>
        {{#each burgers}}
            {{#unless devoured}}
                {{> burgers devoured=false}}
            {{/unless}}
        {{/each}}
        </ul>
    </div>
    <div class="devouredBurgers">
        <ul>
        {{#each burgers}}
            {{#unless devoured}}
                {{> burgers devoured=true}}
            {{/unless}}
        {{/each}}
        </ul>
    </div>
</div>


<div class="form"> 
<form>
    <div>
        <label for="burgerName">Burger Name</label>
        <input type="text" id="burgerInput" name="burgerName">
    </div>

    <button type="submit">Add Burger</button>
</form>
<p>Enter the type of burger you want to eat, then click the "Submit" button.</p>
</div>


<li>
    {{burger_name}}
    <button class="devourBtn" data-id="{{id}}" data-newStatus="{{devoured}}">
        {{#if devoured}}
    </button>
</li>